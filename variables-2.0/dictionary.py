# Create diccionarios con dict() -> .
dict = dict(name="Andres", lastname="Ramirez")

# Create diccionarios con fromkeys() -> Las keys con el value en none (sin definir).
dict = dict.fromkeys(["name", "lastname"])

print(dict)